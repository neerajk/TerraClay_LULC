seed_everything: 42
trainer:
  accelerator: auto
  devices: auto
  precision: 16-mixed
  max_epochs: 50
  default_root_dir: outputs/terratorch_runs_clay
data:
  class_path: terratorch.datamodules.GenericNonGeoSegmentationDataModule
  init_args:
    batch_size: 8
    num_workers: 4
    num_classes: 20
    train_data_root: data/processed/tiles/train/images
    train_label_data_root: data/processed/tiles/train/masks
    val_data_root: data/processed/tiles/val/images
    val_label_data_root: data/processed/tiles/val/masks
    test_data_root: data/processed/tiles/test/images
    test_label_data_root: data/processed/tiles/test/masks
    img_grep: '*.tif'
    label_grep: '*.tif'
    dataset_bands:
    - BLUE
    - GREEN
    - RED
    - NIR_NARROW
    - SWIR_1
    - SWIR_2
    output_bands:
    - BLUE
    - GREEN
    - RED
    - NIR_NARROW
    - SWIR_1
    - SWIR_2
    means: configs/stats/terratorch_segmentation_clay_template.landsat-c2-l2.means.txt
    stds: configs/stats/terratorch_segmentation_clay_template.landsat-c2-l2.stds.txt
    no_data_replace: 0
    no_label_replace: 0
    train_transform:
    - class_path: ToTensorV2
    val_transform:
    - class_path: ToTensorV2
    test_transform:
    - class_path: ToTensorV2
model:
  class_path: terratorch.tasks.SemanticSegmentationTask
  init_args:
    model_factory: ClayModelFactory
    model_args:
      backbone: REPLACE_WITH_VALID_CLAY_BACKBONE
      decoder: FCNDecoder
      in_channels: 6
      num_classes: 20
    loss: ce
    ignore_index: 0
    freeze_backbone: false
optimizer:
  class_path: torch.optim.AdamW
  init_args:
    lr: 0.0001
    weight_decay: 0.0001
